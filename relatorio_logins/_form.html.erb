<%= form_for(relatorio_login) do |f| %>
  <% if relatorio_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relatorio_login.errors.count, "error") %> prohibited this relatorio_login from being saved:</h2>

      <ul>
      <% relatorio_login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email_id %>
    <%= f.text_field :email_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
